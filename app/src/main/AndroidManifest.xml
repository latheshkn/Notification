<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fcmnotification">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".FCMMessageReceiverService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>

            </intent-filter>
        </service>
<!--        to set te notification icon sound and color-->
<!--        <meta-data android:name="com.google.firebas.default_notification_icon"-->
<!--            android:resource="@drawable/speech_bubble"/>-->
<!--        <meta-data android:name="com.google.firebas.default_notification_color"-->
<!--            android:resource="@color/FcmnotificationColor"/>-->
    </application>

</manifest>